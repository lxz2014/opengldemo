package com.lxz.opengl.media.encode;

public class OutputBufferInfo {
    public int outputBufferIndex = -1;
    public int size = 0;

    public OutputBufferInfo(int outputBufferIndex, int size) {
        this.outputBufferIndex = outputBufferIndex;
        this.size = size;
    }
}
